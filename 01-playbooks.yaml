#  name: ping the node
#  hosts: web
#   # this is list of tasks/modules/collections
#  tasks:
#  - name: pinging the server
#     # this is the map
#    ansible.builtin.ping:

name: ping the node
hosts: web
tasks:
 - name: pinging the server
   ansible.builtin.ping: